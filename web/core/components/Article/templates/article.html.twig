<!-- Page Heading -->
<div class="d-sm-flex align-items-center flex-start mb-4">
    <h1 class="h3 mb-0 text-gray-800">{{ page.title }}</h1> <a href="{{ route(['article', 'edit', article.id]) }}" class="btn btn-primary">Edit</a>
</div>
{{ article.content|raw }}